name: build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    uses: immediate-media/github-actions/.github/workflows/php-package-testing.yml@main
    with:
      php-version: '8.3'
    secrets: inherit