name: build

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    uses: immediatemediaco/github-actions/.github/workflows/php-package-testing.yml@main
    with:
      php-version: '8.3'
    secrets: inherit
